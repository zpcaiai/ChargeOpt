[project]
name = "chargeopt"
version = "0.1.0"
description = "ChargeOpt OS Enterprise MVP for multi-station charging energy dispatch and VPP optimization."
requires-python = ">=3.12"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
